# Place server.crt and server.key here. Generate self-signed for dev:
#   openssl req -x509 -nodes -days 365 -newkey rsa:4096 \
#     -keyout nginx/certs/server.key -out nginx/certs/server.crt \
#     -subj "/CN=localhost"
