<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--
      Sheet #76: Accounting resets batches of entries, and clicking the button
      once per document is the whole of the complaint. The action is gated to
      the accounting manager group because a reset keeps the reconciliation
      (Odoo 19) and re-posting recomputes the hand-keyed tax lines.
    -->
    <record id="action_levis_reset_to_draft_server" model="ir.actions.server">
        <field name="name">Reset to Draft</field>
        <field name="model_id" ref="account.model_account_move"/>
        <field name="binding_model_id" ref="account.model_account_move"/>
        <field name="binding_view_types">list</field>
        <!-- Odoo 19 renamed ir.actions.server's groups_id to group_ids. -->
        <field name="group_ids" eval="[(4, ref('account.group_account_manager'))]"/>
        <field name="state">code</field>
        <field name="code">action = records.action_levis_reset_to_draft_selected()</field>
    </record>

</odoo>
