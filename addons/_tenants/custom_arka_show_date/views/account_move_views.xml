<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_move_form_arka_show_date" model="ir.ui.view">
        <field name="name">account.move.form.arka.show.date</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <field name="invoice_date_due" position="after">
                <field name="x_custom_event_visible" invisible="1"/>
                <field name="x_custom_show_date"
                       invisible="move_type not in ('out_invoice', 'out_refund', 'in_invoice', 'in_refund')"/>
                <field name="x_custom_event_name"
                       invisible="not x_custom_event_visible or move_type not in ('out_invoice', 'out_refund', 'in_invoice', 'in_refund')"/>
                <field name="x_custom_event_location"
                       invisible="not x_custom_event_visible or move_type not in ('out_invoice', 'out_refund', 'in_invoice', 'in_refund')"/>
            </field>
            <xpath expr="//header" position="inside">
                <button name="action_custom_event_apply_analytic"
                        type="object"
                        string="Tag Event"
                        invisible="not x_custom_event_visible or state != 'draft'"
                        help="Write this event's analytic account on every product line that has none."/>
            </xpath>
        </field>
    </record>
</odoo>
