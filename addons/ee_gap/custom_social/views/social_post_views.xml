<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_social_post_list" model="ir.ui.view">
        <field name="name">social.post.list</field>
        <field name="model">social.post</field>
        <field name="arch" type="xml">
            <list decoration-success="state == 'published'"
                  decoration-info="state == 'scheduled'"
                  decoration-danger="state == 'failed'"
                  decoration-muted="state == 'cancelled'">
                <field name="account_id"/>
                <field name="scheduled_at"/>
                <field name="published_at"/>
                <field name="state" widget="badge"/>
            </list>
        </field>
    </record>
    <record id="view_social_post_form" model="ir.ui.view">
        <field name="name">social.post.form</field>
        <field name="model">social.post</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_schedule" string="Schedule" type="object" class="btn-primary"
                            invisible="state != 'draft'"/>
                    <button name="action_publish_now" string="Publish Now" type="object"
                            class="btn-primary" invisible="state == 'published'"/>
                    <button name="action_cancel" string="Cancel" type="object"
                            invisible="state in ('published','cancelled')"/>
                    <field name="state" widget="statusbar"/>
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="account_id"/>
                            <field name="scheduled_at"/>
                            <field name="published_at" readonly="1"/>
                        </group>
                        <group>
                            <field name="media_attachment_id"/>
                            <field name="external_post_id" readonly="1"/>
                        </group>
                    </group>
                    <separator string="Body"/>
                    <field name="body"/>
                    <separator string="Last Error" invisible="not last_error"/>
                    <field name="last_error" readonly="1" invisible="not last_error"/>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>
    <record id="action_social_post" model="ir.actions.act_window">
        <field name="name">Posts</field>
        <field name="res_model">social.post</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
