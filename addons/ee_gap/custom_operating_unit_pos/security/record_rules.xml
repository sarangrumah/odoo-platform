<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Same self-neutralising shape as custom_operating_unit_docs: a
             user with no unit assigned is unrestricted, and documents that
             carry no unit stay visible while include_untagged is on.
             domain_force is eval'd as Python, so it must stay on one line. -->

        <record id="rule_pos_config_ou" model="ir.rule">
            <field name="name">Point of Sale: Operating Unit scope</field>
            <field name="model_id" ref="point_of_sale.model_pos_config"/>
            <field name="domain_force">(['|', ('operating_unit_id', '=', False)] if user.ou_include_untagged else []) + [('operating_unit_id', 'in', user.ou_allowed_ids.ids)] if user.ou_is_scoped else [(1, '=', 1)]</field>
        </record>

        <record id="rule_pos_session_ou" model="ir.rule">
            <field name="name">POS Session: Operating Unit scope</field>
            <field name="model_id" ref="point_of_sale.model_pos_session"/>
            <field name="domain_force">(['|', ('operating_unit_id', '=', False)] if user.ou_include_untagged else []) + [('operating_unit_id', 'in', user.ou_allowed_ids.ids)] if user.ou_is_scoped else [(1, '=', 1)]</field>
        </record>

        <record id="rule_pos_order_ou" model="ir.rule">
            <field name="name">POS Order: Operating Unit scope</field>
            <field name="model_id" ref="point_of_sale.model_pos_order"/>
            <field name="domain_force">(['|', ('operating_unit_id', '=', False)] if user.ou_include_untagged else []) + [('operating_unit_id', 'in', user.ou_allowed_ids.ids)] if user.ou_is_scoped else [(1, '=', 1)]</field>
        </record>
    </data>
</odoo>
