<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Declared as product.product, like core hr_expense's own expense
             products: the template is created implicitly, and the xmlid then
             points at the variant, which is what hr.expense.product_id needs.
             default_code MILEAGE is what _compute_is_mileage matches on. -->
        <record id="product_expense_mileage" model="product.product">
            <field name="name">Mileage</field>
            <field name="default_code">MILEAGE</field>
            <field name="type">service</field>
            <field name="sale_ok" eval="False"/>
            <field name="purchase_ok" eval="False"/>
            <field name="can_be_expensed" eval="True"/>
            <field name="list_price">5000.0</field>
            <field name="standard_price">5000.0</field>
        </record>
    </data>
</odoo>
