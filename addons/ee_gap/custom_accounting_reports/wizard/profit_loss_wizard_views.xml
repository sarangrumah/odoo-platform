<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_profit_loss_wizard_form" model="ir.ui.view">
        <field name="name">custom.report.profit.loss.wizard.form</field>
        <field name="model">custom.report.profit.loss.wizard</field>
        <field name="arch" type="xml">
            <form string="Profit &amp; Loss">
                <group>
                    <group>
                        <field name="date_from"/>
                        <field name="date_to"/>
                        <field name="posted_only"/>
                        <field name="comparison"/>
                    </group>
                    <group>
                        <field name="company_ids" widget="many2many_tags"/>
                    </group>
                </group>
                <div class="text-muted">
                    "by Branch" reports the same lines with one amount column per
                    Operating Unit. Journal items carrying no Operating Unit are
                    reported under the head-office column.
                </div>
                <footer>
                    <button string="View" name="action_view"
                            type="object" class="btn-primary"/>
                    <button string="View by Branch" name="action_view_by_branch"
                            type="object" class="btn-secondary"/>
                    <button string="Print PDF" name="action_print"
                            type="object" class="btn-secondary"/>
                    <button string="Export Excel" name="action_export_xlsx"
                            type="object" class="btn-secondary"/>
                    <button string="Export by Branch" name="action_export_xlsx_by_branch"
                            type="object" class="btn-secondary"/>
                    <button string="Cancel" class="btn-secondary"
                            special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_profit_loss_wizard" model="ir.actions.act_window">
        <field name="name">Profit &amp; Loss</field>
        <field name="res_model">custom.report.profit.loss.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
</odoo>
