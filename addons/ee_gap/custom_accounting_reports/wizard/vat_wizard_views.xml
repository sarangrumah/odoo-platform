<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_vat_wizard_form" model="ir.ui.view">
        <field name="name">custom.report.vat.wizard.form</field>
        <field name="model">custom.report.vat.wizard</field>
        <field name="arch" type="xml">
            <form string="Report VAT">
                <group>
                    <group>
                        <field name="vat_side"/>
                        <field name="date_from"/>
                        <field name="date_to"/>
                        <field name="posted_only"/>
                    </group>
                    <group>
                        <field name="company_ids" widget="many2many_tags"/>
                        <field name="account_ids" widget="many2many_tags"/>
                        <field name="partner_ids" widget="many2many_tags"/>
                    </group>
                </group>
                <footer>
                    <button string="View" name="action_view"
                            type="object" class="btn-primary"/>
                    <button string="Print PDF" name="action_print"
                            type="object" class="btn-secondary"/>
                    <button string="Export Excel" name="action_export_xlsx"
                            type="object" class="btn-secondary"/>
                    <button string="Lihat Transaksi" name="action_view_source"
                            type="object" class="btn-secondary"/>
                    <button string="Cancel" class="btn-secondary"
                            special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_vat_wizard" model="ir.actions.act_window">
        <field name="name">Report VAT (Invoice PPN)</field>
        <field name="res_model">custom.report.vat.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
</odoo>
