<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_account_payment_form_batch" model="ir.ui.view">
        <field name="name">account.payment.form.batch</field>
        <field name="model">account.payment</field>
        <field name="inherit_id" ref="account.view_account_payment_form"/>
        <field name="arch" type="xml">
            <xpath expr="//sheet/group" position="inside">
                <group>
                    <field name="batch_payment_id" readonly="1"
                           invisible="not batch_payment_id"/>
                </group>
            </xpath>
        </field>
    </record>

    <!-- "Add to Batch" in the payments list Actions menu -->
    <record id="action_payments_create_batch" model="ir.actions.server">
        <field name="name">Add to Batch</field>
        <field name="model_id" ref="account.model_account_payment"/>
        <field name="binding_model_id" ref="account.model_account_payment"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">action = model.action_create_batch_from_selection()</field>
    </record>

</odoo>
