<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_knowledge_tag_list" model="ir.ui.view">
        <field name="name">knowledge.tag.list</field>
        <field name="model">knowledge.tag</field>
        <field name="arch" type="xml">
            <list string="Tags" editable="bottom">
                <field name="name"/>
                <field name="color" widget="color_picker"/>
                <field name="active"/>
            </list>
        </field>
    </record>

    <record id="view_knowledge_tag_form" model="ir.ui.view">
        <field name="name">knowledge.tag.form</field>
        <field name="model">knowledge.tag</field>
        <field name="arch" type="xml">
            <form string="Tag">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="color" widget="color_picker"/>
                        <field name="active"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_knowledge_tag" model="ir.actions.act_window">
        <field name="name">Tags</field>
        <field name="res_model">knowledge.tag</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
