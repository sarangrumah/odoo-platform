<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_retail_import_feed_list" model="ir.ui.view">
        <field name="name">retail.import.feed.list</field>
        <field name="model">retail.import.feed</field>
        <field name="arch" type="xml">
            <list string="Feeds" decoration-success="last_status=='ok'" decoration-danger="last_status=='error'">
                <field name="name"/>
                <field name="profile_id"/>
                <field name="protocol"/>
                <field name="host"/>
                <field name="remote_dir"/>
                <field name="file_glob"/>
                <field name="last_run"/>
                <field name="last_status"/>
                <field name="files_imported"/>
                <field name="active" widget="boolean_toggle"/>
            </list>
        </field>
    </record>

    <record id="view_retail_import_feed_form" model="ir.ui.view">
        <field name="name">retail.import.feed.form</field>
        <field name="model">retail.import.feed</field>
        <field name="arch" type="xml">
            <form string="Feed">
                <header>
                    <button name="action_test_connection" type="object" string="Test Connection"/>
                    <button name="action_poll_now" type="object" string="Poll Now" class="btn-primary"/>
                </header>
                <sheet>
                    <group>
                        <group string="Source">
                            <field name="name"/>
                            <field name="profile_id"/>
                            <field name="company_id" groups="base.group_multi_company"/>
                            <field name="active"/>
                            <field name="run_async"/>
                            <field name="post_action"/>
                            <field name="archive_dir" invisible="post_action != 'move'"/>
                        </group>
                        <group string="Connection">
                            <field name="protocol"/>
                            <field name="host"/>
                            <field name="port"/>
                            <field name="username"/>
                            <field name="auth_type" invisible="protocol != 'sftp'"/>
                            <field name="password" password="True" invisible="auth_type == 'key' and protocol == 'sftp'"/>
                            <field name="password_param"/>
                            <field name="private_key_path" invisible="protocol != 'sftp' or auth_type != 'key'"/>
                            <field name="passive" invisible="protocol == 'sftp'"/>
                            <field name="remote_dir"/>
                            <field name="file_glob"/>
                        </group>
                    </group>
                    <group string="Last run">
                        <field name="last_run"/>
                        <field name="last_status"/>
                        <field name="files_imported"/>
                        <field name="last_message"/>
                    </group>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="action_retail_import_feed" model="ir.actions.act_window">
        <field name="name">Feeds</field>
        <field name="res_model">retail.import.feed</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
