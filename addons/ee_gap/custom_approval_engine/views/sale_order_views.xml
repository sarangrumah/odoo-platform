<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_sale_order_form_approval" model="ir.ui.view">
        <field name="name">sale.order.form.approval</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button name="action_request_approval"
                        string="Request Approval"
                        type="object"
                        class="btn-primary"
                        invisible="x_custom_approval_state in ('pending','approved') or state not in ('draft','sent')"/>
                <button name="action_open_approval_request"
                        string="View Approval"
                        type="object"
                        invisible="not x_custom_approval_request_id"/>
            </xpath>
            <xpath expr="//field[@name='state']" position="before">
                <field name="x_custom_approval_state" widget="badge"
                       invisible="not x_custom_approval_state"/>
            </xpath>
        </field>
    </record>

</odoo>
