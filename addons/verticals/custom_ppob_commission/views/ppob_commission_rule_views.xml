<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_ppob_commission_rule_list" model="ir.ui.view">
        <field name="name">custom.ppob.commission.rule.list</field>
        <field name="model">custom.ppob.commission.rule</field>
        <field name="arch" type="xml">
            <list string="Commission Rules">
                <field name="priority" widget="handle"/>
                <field name="name"/>
                <field name="scope"/>
                <field name="class_id"/>
                <field name="product_id"/>
                <field name="partner_id"/>
                <field name="calc_type"/>
                <field name="rate"/>
                <field name="active" widget="boolean_toggle"/>
            </list>
        </field>
    </record>

    <record id="view_ppob_commission_rule_form" model="ir.ui.view">
        <field name="name">custom.ppob.commission.rule.form</field>
        <field name="model">custom.ppob.commission.rule</field>
        <field name="arch" type="xml">
            <form string="Commission Rule">
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="scope"/>
                            <field name="priority"/>
                            <field name="active"/>
                        </group>
                        <group>
                            <field name="class_id"/>
                            <field name="product_id"/>
                            <field name="partner_id"/>
                        </group>
                    </group>
                    <group>
                        <group>
                            <field name="calc_type"/>
                            <field name="rate"/>
                        </group>
                        <group>
                            <field name="valid_from"/>
                            <field name="valid_to"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_ppob_commission_rule" model="ir.actions.act_window">
        <field name="name">Commission Rules</field>
        <field name="res_model">custom.ppob.commission.rule</field>
        <field name="view_mode">list,form</field>
    </record>

</odoo>
