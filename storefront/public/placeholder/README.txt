Placeholder assets only.

Do NOT place imagery copied from gentlewomanonline.com or any third party here
— that would be a copyright violation. Use CC0 / self-authored / licensed
photography, or let product imagery come from Odoo at runtime
(/web/image/product.template/<id>/image_*).

The storefront renders graceful empty states (sand-coloured blocks) when an
image is missing, so the app runs fine with this folder empty.
